export enum PaymentPlatForm {
  EasyPaisa = "EazyPaisa",
  JazzCash = "JazzCash",
}

export enum LibraryTabs {
  activity = "activity",
  books = "books",
  quizzes = "quizzes",
  wishlist = "wishlist",
}
